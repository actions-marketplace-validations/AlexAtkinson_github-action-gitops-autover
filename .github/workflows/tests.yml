name: tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tests:
    name: Run Isolated Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: bash scripts/tests.sh
